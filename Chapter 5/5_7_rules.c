# include <stdio.h>

int main(){
    // Program list 5.7
    printf("Program list 5.7:\n");
    int top, score;

    top = score = -(2+5)*6 + (4+3*(2+3));
    printf("top = %d, score = %d\n", top, score);

    return 0;
}